import MongodbWrapper from "../../../../database/mongo/mongo.wrapper.js";
import { headSchema } from "../head.schema.js";

export default async function importHead(req, res) {
  return await wrapper.import(coaArr);
}
